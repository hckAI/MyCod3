#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */  
    int N;
    //int *array;
    cin>>N;
    vector<int>V(N);
    for(vector<int>::size_type i=0; i<N; i++)
    cin>>V[i];
    sort(V.begin(),V.end());
    for(int i=0; i<N; i++)
     cout<<V[i]<<"\n";
     
     //cout<<V
    return 0;
}
